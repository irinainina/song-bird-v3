import HomePage from '../scenes/HomePage/HomePage';
import Head from 'next/head';

const Home = () => (
  <>
    <Head>
      <title>songbird</title>
    </Head>
    <HomePage />
  </>
);

export default Home;
